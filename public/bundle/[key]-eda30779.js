
(function(l, r) { if (l.getElementById('livereloadscript')) return; r = l.createElement('script'); r.async = 1; r.src = '//' + (window.location.host || 'localhost').split(':')[0] + ':35729/livereload.js?snipver=1'; r.id = 'livereloadscript'; l.head.appendChild(r) })(window.document);
import { S as SvelteComponentDev, a as init, s as safe_not_equal, d as dispatch_dev, v as validate_store, c as component_subscribe, z as element, D as text, E as attr_dev, A as add_location, y as listen_dev, b as insert_dev, C as append_dev, G as set_data_dev, O as add_render_callback, P as create_in_transition, R as create_bidirectional_transition, Q as create_out_transition, j as detach_dev } from './index-4e59569a.js';
import { g as goto } from './index-5bd4e51a.js';

/* src\pages\example\modal\animated\[key].svelte generated by Svelte v3.16.4 */
const file = "src\\pages\\example\\modal\\animated\\[key].svelte";

function create_fragment(ctx) {
	let div1;
	let div0;
	let t;
	let div0_intro;
	let div0_outro;
	let div1_transition;
	let current;
	let dispose;

	const block = {
		c: function create() {
			div1 = element("div");
			div0 = element("div");
			t = text(/*key*/ ctx[0]);
			attr_dev(div0, "class", "modal");
			add_location(div0, file, 10, 2, 286);
			attr_dev(div1, "class", "container");
			add_location(div1, file, 9, 0, 207);
			dispose = listen_dev(div1, "click", /*click_handler*/ ctx[6], false, false, false);
		},
		l: function claim(nodes) {
			throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		},
		m: function mount(target, anchor) {
			insert_dev(target, div1, anchor);
			append_dev(div1, div0);
			append_dev(div0, t);
			current = true;
		},
		p: function update(ctx, dirty) {
			if (!current || dirty[0] & /*key*/ 1) set_data_dev(t, /*key*/ ctx[0]);
		},
		i: function intro(local) {
			if (current) return;

			if (local) {
				add_render_callback(() => {
					if (div0_outro) div0_outro.end(1);
					if (!div0_intro) div0_intro = create_in_transition(div0, /*receive*/ ctx[3], { key: "modal" });
					div0_intro.start();
				});
			}

			if (local) {
				add_render_callback(() => {
					if (!div1_transition) div1_transition = create_bidirectional_transition(div1, /*fade*/ ctx[4], {}, true);
					div1_transition.run(1);
				});
			}

			current = true;
		},
		o: function outro(local) {
			if (div0_intro) div0_intro.invalidate();

			if (local) {
				div0_outro = create_out_transition(div0, /*send*/ ctx[2], { key: "modal" });
			}

			if (local) {
				if (!div1_transition) div1_transition = create_bidirectional_transition(div1, /*fade*/ ctx[4], {}, false);
				div1_transition.run(0);
			}

			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(div1);
			if (detaching && div0_outro) div0_outro.end();
			if (detaching && div1_transition) div1_transition.end();
			dispose();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

function instance($$self, $$props, $$invalidate) {
	let $goto;
	validate_store(goto, "goto");
	component_subscribe($$self, goto, $$value => $$invalidate(1, $goto = $$value));
	let { key } = $$props, { scoped } = $$props;
	const { send, receive, fade } = scoped;
	const writable_props = ["key", "scoped"];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== "$$") console.warn(`<U5Bkeyu5D> was created with unknown prop '${key}'`);
	});

	const click_handler = () => $goto("../");

	$$self.$set = $$props => {
		if ("key" in $$props) $$invalidate(0, key = $$props.key);
		if ("scoped" in $$props) $$invalidate(5, scoped = $$props.scoped);
	};

	$$self.$capture_state = () => {
		return { key, scoped, $goto };
	};

	$$self.$inject_state = $$props => {
		if ("key" in $$props) $$invalidate(0, key = $$props.key);
		if ("scoped" in $$props) $$invalidate(5, scoped = $$props.scoped);
		if ("$goto" in $$props) goto.set($goto = $$props.$goto);
	};

	return [key, $goto, send, receive, fade, scoped, click_handler];
}

class U5Bkeyu5D extends SvelteComponentDev {
	constructor(options) {
		super(options);
		init(this, options, instance, create_fragment, safe_not_equal, { key: 0, scoped: 5 });

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "U5Bkeyu5D",
			options,
			id: create_fragment.name
		});

		const { ctx } = this.$$;
		const props = options.props || ({});

		if (/*key*/ ctx[0] === undefined && !("key" in props)) {
			console.warn("<U5Bkeyu5D> was created without expected prop 'key'");
		}

		if (/*scoped*/ ctx[5] === undefined && !("scoped" in props)) {
			console.warn("<U5Bkeyu5D> was created without expected prop 'scoped'");
		}
	}

	get key() {
		throw new Error("<U5Bkeyu5D>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set key(value) {
		throw new Error("<U5Bkeyu5D>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	get scoped() {
		throw new Error("<U5Bkeyu5D>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set scoped(value) {
		throw new Error("<U5Bkeyu5D>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}
}

export default U5Bkeyu5D;
//# sourceMappingURL=[key]-eda30779.js.map
